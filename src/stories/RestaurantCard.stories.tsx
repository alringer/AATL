import { storiesOf } from '@storybook/react'
import React from 'react'

export const RestaurantCardData = {
    default: {},
}

storiesOf('Restaurant Card', module).add('Default Restaurant Card', () => {
    return <div>This is Restaurant Card story</div>
})
