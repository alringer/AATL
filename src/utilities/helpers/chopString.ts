export const chopStringFullRecommendationDescription = (input: string) => {
    return input.length > 141 ? `${input.slice(0, 141)}...` : input
}

export const chopStringRecommendationTitle = (input: string) => {
    return input.length > 57 ? `${input.slice(0, 57)}...` : input
}

export const chopStringSimpleRecommendationDescription = (input: string) => {
    return input.length > 250 ? `${input.slice(0, 250)}...` : input
}

export const chopStringSmallCallToAction = (input: string) => {
    return input.length > 65 ? `${input.slice(0, 65)}...` : input
}

export const chopStringSmallPlaceName = (input: string) => {
    return input.length > 20 ? `${input.slice(0, 20)}...` : input
}

export const chopStringSmallPlaceDescription = (input: string) => {
    return input.length > 90 ? `${input.slice(0, 90)}...` : input
}

export const chopStringInfiniteCarouselCardDescription = (input: string) => {
    return input.length > 140 ? `${input.slice(0, 140)}...` : input
}

export const chopStringPlaceNameLaptop = (input: string) => {
    return input.length > 29 ? `${input.slice(0, 29)}...` : input
}

export const chopStringSearchPlaceNameLaptop = (input: string) => {
    return input.length > 18 ? `${input.slice(0, 18)}...` : input
}

export const chopStringPlaceNameTablet = (input: string) => {
    return input.length > 20 ? `${input.slice(0, 20)}...` : input
}

export const chopStringSearchPlaceNameTablet = (input: string) => {
    return input.length > 24 ? `${input.slice(0, 24)}...` : input
}

export const chopStringPlaceNameMobile = (input: string) => {
    return input.length > 40 ? `${input.slice(0, 40)}...` : input
}

export const chopStringPlaceUserNameLaptop = (input: string) => {
    return input.length > 42 ? `${input.slice(0, 42)}...` : input
}

export const chopStringPlaceUserNameTablet = (input: string) => {
    return input.length > 36 ? `${input.slice(0, 36)}...` : input
}

export const chopStringPlaceUserNameMobile = (input: string) => {
    return input.length > 30 ? `${input.slice(0, 30)}...` : input
}

export const chopStringPlaceCategoriesLaptop = (input: string) => {
    return input.length > 67 ? `${input.slice(0, 67)}...` : input
}

export const chopStringSearchPlaceCategoriesLaptop = (input: string) => {
    return input.length > 49 ? `${input.slice(0, 49)}...` : input
}

export const chopStringPlaceCategoriesTablet = (input: string) => {
    return input.length > 50 ? `${input.slice(0, 50)}...` : input
}

export const chopStringSearchPlaceCategoriesTablet = (input: string) => {
    return input.length > 56 ? `${input.slice(0, 56)}...` : input
}

export const chopStringPlaceCategoriesMobile = (input: string) => {
    return input.length > 34 ? `${input.slice(0, 34)}...` : input
}

export const chopStringPlaceUserByLineLaptop = (input: string) => {
    return input.length > 42 ? `${input.slice(0, 42)}...` : input
}

export const chopStringPlaceUserByLineTablet = (input: string) => {
    return input.length > 42 ? `${input.slice(0, 42)}...` : input
}

export const chopStringPlaceUserByLineMobile = (input: string) => {
    return input.length > 30 ? `${input.slice(0, 30)}...` : input
}

export const chopStringPlaceLatestRecommendationTitleLaptop = (input: string) => {
    return input.length > 55 ? `${input.slice(0, 55)}...` : input
}

export const chopStringSearchPlaceLatestRecommendationTitleLaptop = (input: string) => {
    return input.length > 40 ? `${input.slice(0, 40)}...` : input
}

export const chopStringPlaceLatestRecommendationTitleTablet = (input: string) => {
    return input.length > 35 ? `${input.slice(0, 35)}...` : input
}

export const chopStringSearchPlaceLatestRecommendationTitleTablet = (input: string) => {
    return input.length > 40 ? `${input.slice(0, 40)}...` : input
}

export const chopStringPlaceLatestRecommendationTitleMobile = (input: string) => {
    return input.length > 30 ? `${input.slice(0, 30)}...` : input
}

export const chopStringPlaceLatestRecommendationContentLaptop = (input: string) => {
    return input.length > 234 ? `${input.slice(0, 234)}...` : input
}

export const chopStringSearchPlaceLatestRecommendationContentLaptop = (input: string) => {
    return input.length > 170 ? `${input.slice(0, 170)}...` : input
}

export const chopStringPlaceLatestRecommendationContentTablet = (input: string) => {
    return input.length > 156 ? `${input.slice(0, 156)}...` : input
}

export const chopStringSearchPlaceLatestRecommendationContentTablet = (input: string) => {
    return input.length > 171 ? `${input.slice(0, 171)}...` : input
}

export const chopStringPlaceLatestRecommendationContentMobile = (input: string) => {
    return input.length > 120 ? `${input.slice(0, 120)}...` : input
}
